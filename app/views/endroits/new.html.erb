<%= form_for(@endroits) do |f| %>

	<%= f.text_field :nom, placeholder: "Entrez un nom" %>

	<%= f.text_field :adresse, placeholder: "Entrez une adresse" %>

	<%= f.submit "Enregistrer", class: "btn btn-info btn-md" %>

<% end %>